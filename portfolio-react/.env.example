# Copy this file to .env.local and fill in your value.

# Google OAuth Client ID for admin "Sign in with Google".
# Get it from Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID.
# Must match GOOGLE_CLIENT_ID in backend/.env.
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_OAUTH_CLIENT_ID
